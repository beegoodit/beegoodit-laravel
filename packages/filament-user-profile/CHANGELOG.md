# Changelog

All notable changes to `filament-user-profile` will be documented in this file.

## [Unreleased]

### Added
- Initial release (Phase 1)
- `Profile` page with heading and basic structure
- `Password` page with heading and basic structure
- `Appearance` page with heading and basic structure
- `TwoFactor` page with heading and basic structure
- `UserProfile` facade for user menu items
- Conditional Two-Factor menu item registration (only if Fortify 2FA enabled)

